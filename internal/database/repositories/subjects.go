package repositories

func GetAllSubjects() {
	// database.Run(query) (result any, err error) {
	// 	row := conn.QueryRow(context.Background(), "select 'Hello!'")
	// 	err = row.Scan(&result)
	// 	if err != nil {
	// 		fmt.Printf("Error: %v\n", err)
	// 		return nil, err
	// 	}
	// 	return
	// })
}
